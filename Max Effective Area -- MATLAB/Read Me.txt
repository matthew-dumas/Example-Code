process_csv.m is a MATLAB script. It scans a directory for CSV files, then processes them individually. 

max_effective_area.m is a MATLAB script that computes an effective area (antenna aperature area) for each set of data in a CSV file. Then it saves the computations for output as a CSV for use in Excel. 