process_csv.m is a MATLAB script. It scans a directory for CSV files, then processes them individually. 

plot_normalized_pattern.m is a MATLAB script that normalizes the data in the CSV file, and then plots the normalized pattern for human review.